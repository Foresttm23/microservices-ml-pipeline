[project]
name = "coursework-ml-microservices"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
]

[dependency-groups]
dev = [
    "pyright>=1.1.409",
    "ruff>=0.15.12",
]

[tool.uv.workspace]
members = [
    "gateway",
    "ml_worker",
    "orchestrator",
]
