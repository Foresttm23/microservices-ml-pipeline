[project]
name = "coursework-ml-microservices"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [

]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "ty>=0.0.32",
]

[tool.uv.workspace]
members = [
    "gateway",
    "ml_worker",
    "orchestrator",
]
