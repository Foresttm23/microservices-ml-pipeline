[project]
name = "shared"
version = "0.1.0"
description = "Shared cross-service contracts and utilities"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.3",
]

[tool.uv]
managed = true
package = true

